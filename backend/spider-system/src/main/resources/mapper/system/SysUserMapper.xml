<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spider.media.system.mapper.SysUserMapper">

    <resultMap id="BaseResultMap" type="com.spider.media.system.entity.SysUser">
        <id column="user_id" property="userId"/>
        <result column="user_name" property="userName"/>
        <result column="nick_name" property="nickName"/>
        <result column="email" property="email"/>
        <result column="phonenumber" property="phonenumber"/>
        <result column="avatar" property="avatar"/>
        <result column="password" property="password"/>
        <result column="status" property="status"/>
        <result column="role" property="role"/>
        <result column="create_by" property="createBy"/>
        <result column="create_time" property="createTime"/>
        <result column="update_by" property="updateBy"/>
        <result column="update_time" property="updateTime"/>
        <result column="remark" property="remark"/>
        <result column="del_flag" property="delFlag"/>
    </resultMap>

    <select id="selectByUserName" resultMap="BaseResultMap">
        SELECT * FROM sys_user
        WHERE user_name = #{userName} AND del_flag = '0'
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO sys_user (user_name, nick_name, email, password, status, role,
                              create_by, create_time)
        VALUES (#{userName}, #{nickName}, #{email}, #{password}, #{status}, #{role},
                #{createBy}, #{createTime})
    </insert>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT * FROM sys_user WHERE del_flag = '0' ORDER BY create_time DESC
    </select>

    <update id="update">
        UPDATE sys_user
        SET nick_name = #{nickName}, email = #{email}, phonenumber = #{phonenumber},
            status = #{status}, role = #{role}, update_by = #{updateBy}, update_time = #{updateTime}
        WHERE user_id = #{userId}
    </update>

    <update id="deleteById">
        UPDATE sys_user SET del_flag = '2' WHERE user_id = #{userId}
    </update>

</mapper>
