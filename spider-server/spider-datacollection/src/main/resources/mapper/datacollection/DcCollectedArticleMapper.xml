<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spider.media.datacollection.mapper.DcCollectedArticleMapper">

    <resultMap id="BaseResultMap" type="com.spider.media.datacollection.entity.DcCollectedArticle">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="target_account_id" property="targetAccountId"/>
        <result column="platform" property="platform"/>
        <result column="title" property="title"/>
        <result column="content" property="content"/>
        <result column="summary" property="summary"/>
        <result column="url" property="url"/>
        <result column="author" property="author"/>
        <result column="view_count" property="viewCount"/>
        <result column="like_count" property="likeCount"/>
        <result column="comment_count" property="commentCount"/>
        <result column="share_count" property="shareCount"/>
        <result column="publish_time" property="publishTime"/>
        <result column="collected_time" property="collectedTime"/>
        <result column="create_by" property="createBy"/>
        <result column="create_time" property="createTime"/>
        <result column="update_by" property="updateBy"/>
        <result column="update_time" property="updateTime"/>
        <result column="remark" property="remark"/>
        <result column="del_flag" property="delFlag"/>
    </resultMap>

    <!-- 显式列清单，避免 SELECT * 带来的列顺序依赖和无效字段传输 -->
    <sql id="Base_Column_List">
        id, user_id, target_account_id, platform, title, content, summary, url, author,
        view_count, like_count, comment_count, share_count, publish_time, collected_time,
        create_by, create_time, update_by, update_time, remark, del_flag
    </sql>

    <select id="selectPage" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM dc_collected_article
        <where>
            del_flag = '0'
            <if test="targetAccountId != null">
                AND target_account_id = #{targetAccountId}
            </if>
            <if test="platform != null and platform != ''">
                AND platform = #{platform}
            </if>
            <if test="title != null and title != ''">
                <!-- 转义 LIKE 通配符（%、_、\），防止用户输入干扰查询语义 -->
                <bind name="titlePattern" value="'%' + title.replaceAll('[%_\\\\]', '\\\\$0') + '%'" />
                AND title LIKE #{titlePattern} ESCAPE '\\'
            </if>
        </where>
        ORDER BY collected_time DESC
    </select>

    <select id="selectByUrl" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM dc_collected_article WHERE url = #{url} AND del_flag = '0' LIMIT 1
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO dc_collected_article (user_id, target_account_id, platform, title, content, summary,
                                          url, author, view_count, like_count, comment_count, share_count,
                                          publish_time, collected_time, create_by, create_time)
        VALUES (#{userId}, #{targetAccountId}, #{platform}, #{title}, #{content}, #{summary},
                #{url}, #{author}, #{viewCount}, #{likeCount}, #{commentCount}, #{shareCount},
                #{publishTime}, #{collectedTime}, #{createBy}, #{createTime})
    </insert>

    <update id="deleteById">
        UPDATE dc_collected_article SET del_flag = '2' WHERE id = #{id} AND del_flag = '0'
    </update>

    <insert id="batchInsert">
        INSERT INTO dc_collected_article (user_id, target_account_id, platform, title, content, summary,
                                          url, author, view_count, like_count, comment_count, share_count,
                                          publish_time, collected_time, create_by, create_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.userId}, #{item.targetAccountId}, #{item.platform}, #{item.title}, #{item.content}, #{item.summary},
             #{item.url}, #{item.author}, #{item.viewCount}, #{item.likeCount}, #{item.commentCount}, #{item.shareCount},
             #{item.publishTime}, #{item.collectedTime}, #{item.createBy}, #{item.createTime})
        </foreach>
    </insert>

</mapper>
