<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spider.media.system.mapper.SysConfigMapper">

    <resultMap id="BaseResultMap" type="com.spider.media.system.entity.SysConfig">
        <id column="id" property="id"/>
        <result column="config_name" property="configName"/>
        <result column="config_key" property="configKey"/>
        <result column="config_value" property="configValue"/>
        <result column="config_type" property="configType"/>
        <result column="create_by" property="createBy"/>
        <result column="create_time" property="createTime"/>
        <result column="update_by" property="updateBy"/>
        <result column="update_time" property="updateTime"/>
        <result column="remark" property="remark"/>
        <result column="del_flag" property="delFlag"/>
    </resultMap>

    <!-- 显式列清单，避免 SELECT * 带来的列顺序依赖和无效字段传输 -->
    <sql id="Base_Column_List">
        id, config_name, config_key, config_value, config_type,
        create_by, create_time, update_by, update_time, remark, del_flag
    </sql>

    <select id="selectByGroup" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM sys_config
        WHERE del_flag = '0'
        <if test="configKey != null and configKey != ''">
            <!-- 前缀匹配：转义 LIKE 通配符（%、_、\），防止用户输入干扰查询语义 -->
            <bind name="configKeyPattern" value="configKey.replaceAll('[%_\\\\]', '\\\\$0') + '%'" />
            AND config_key LIKE #{configKeyPattern} ESCAPE '\\'
        </if>
        ORDER BY id
    </select>

    <select id="selectByKey" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM sys_config
        WHERE config_key = #{configKey} AND del_flag = '0'
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_config (config_name, config_key, config_value, config_type,
                                create_by, create_time)
        VALUES (#{configName}, #{configKey}, #{configValue}, #{configType},
                #{createBy}, #{createTime})
    </insert>

    <update id="update">
        UPDATE sys_config
        SET config_value = #{configValue},
            update_by = #{updateBy},
            update_time = #{updateTime}
        WHERE id = #{id} AND del_flag = '0'
    </update>

    <update id="deleteById">
        UPDATE sys_config SET del_flag = '2' WHERE id = #{id}
    </update>

</mapper>
