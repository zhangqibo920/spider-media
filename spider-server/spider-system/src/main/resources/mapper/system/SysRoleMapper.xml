<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spider.media.system.mapper.SysRoleMapper">

    <resultMap id="BaseResultMap" type="com.spider.media.system.entity.SysRole">
        <id column="role_id" property="roleId"/>
        <result column="role_name" property="roleName"/>
        <result column="role_key" property="roleKey"/>
        <result column="status" property="status"/>
        <result column="create_by" property="createBy"/>
        <result column="create_time" property="createTime"/>
        <result column="update_by" property="updateBy"/>
        <result column="update_time" property="updateTime"/>
        <result column="remark" property="remark"/>
        <result column="del_flag" property="delFlag"/>
    </resultMap>

    <sql id="Base_Column_List">
        role_id, role_name, role_key, status, create_by, create_time, update_by, update_time, remark, del_flag
    </sql>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM sys_role WHERE del_flag = '0' ORDER BY role_id
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM sys_role WHERE role_id = #{roleId} AND del_flag = '0'
    </select>

    <select id="selectByKey" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM sys_role WHERE role_key = #{roleKey} AND del_flag = '0'
    </select>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT r.* FROM sys_role r
        INNER JOIN sys_user_role ur ON r.role_id = ur.role_id
        WHERE ur.user_id = #{userId} AND r.del_flag = '0' AND r.status = '0'
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="roleId">
        INSERT INTO sys_role (role_name, role_key, status, create_by, create_time, remark)
        VALUES (#{roleName}, #{roleKey}, #{status}, #{createBy}, #{createTime}, #{remark})
    </insert>

    <update id="update">
        UPDATE sys_role
        SET role_name = #{roleName}, role_key = #{roleKey}, status = #{status},
            update_by = #{updateBy}, update_time = #{updateTime}, remark = #{remark}
        WHERE role_id = #{roleId}
    </update>

    <update id="deleteById">
        UPDATE sys_role SET del_flag = '2' WHERE role_id = #{roleId}
    </update>

</mapper>
